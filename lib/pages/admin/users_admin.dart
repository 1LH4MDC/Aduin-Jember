import 'package:flutter/material.dart';

class UsersAdmin extends StatelessWidget {
  const UsersAdmin({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Manajemen Users')));
  }
}