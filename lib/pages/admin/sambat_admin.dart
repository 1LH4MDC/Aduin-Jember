import 'package:flutter/material.dart';

import 'admin_sambat_page.dart';

class SambatAdmin extends StatelessWidget {
  const SambatAdmin({super.key});

  @override
  Widget build(BuildContext context) {
    return const AdminSambatPage();
  }
}
